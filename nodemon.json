{
    "watch": ["src/server"],
    "ext": "ts",
    "exec": "npm run build:server:dev && node --enable-source-maps --inspect=9229 dist/webserver/server.cjs",
    "ignore": ["dist/*"],
    "env": {
        "NODE_ENV": "development"
    }
}
